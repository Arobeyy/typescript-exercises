const superHeros: string[] = [];
//const heroHP: number[] = [];
const heroHP: Array<number> = [];

superHeros.push("Ruber Ducky");
heroHP.push(10);

type myUser = {
  name: string;
  isActive: boolean;
};

const allUsers: myUser[] = [];

allUsers.push({ name: "su", isActive: true });

const MLModels: number[][] = [
  [255, 255, 255],
  [255, 256, 255],
];
